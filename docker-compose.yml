services:
  backend:
    build: ./backend
    restart: always
    env_file:
      - ./backend/.env.example
    environment:
      - HOST=0.0.0.0
      - PORT=8004
      - MEAL_PLANNER_API_KEY=${MEAL_PLANNER_API_KEY}
      - MEAL_PLANNER_API_PROTECT_ALL=${MEAL_PLANNER_API_PROTECT_ALL:-1}
      - MEAL_PLANNER_PDFS_PUBLIC=${MEAL_PLANNER_PDFS_PUBLIC:-0}
      - CORS_ORIGINS=${CORS_ORIGINS:-http://localhost:8003,http://127.0.0.1:8003}
    ports:
      - "8004:8004"
    volumes:
      - ./backend:/usr/src/app
      - /usr/src/app/node_modules
    command: sh -c "npm install && npm run dev"

  frontend:
    build: ./frontend
    restart: always
    depends_on:
      - backend
    environment:
      - VITE_API_URL=http://localhost:8004/api
    ports:
      - "8003:8003"
    volumes:
      - ./frontend:/usr/src/app
      - /usr/src/app/node_modules
    command: sh -c "npm install && npm run dev"
